== README

The rails server uses Puma and requires multiple workers in development to run the CSV and PDF reports.  To start the web server like this use the following command parameters:

<tt>bundle exec rails s Puma</tt>
